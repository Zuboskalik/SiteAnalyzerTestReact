# Semantic Relevance & Space Analyzer — Чек-лист реализации

Атомарные задачи реализации на основе [docs/spec.md](./spec.md) и [docs/plan.md](./plan.md). Каждая задача содержит ссылку на затрагиваемые файлы (по структуре из плана) и Definition of Done (DoD) — проверяемое условие завершённости.

Формат ID: `P<фаза>-<номер>`.

## Отклонения от плана (Phase 1–2)

Фактическая реализация расходится с `docs/plan.md` в следующих пунктах; последующие фазы должны опираться на этот список:

- **React 19** (шаблон Vite) вместо React 18 из плана.
- **TailwindCSS v4** подключён через `@tailwindcss/vite` (плагин в `vite.config.js` + `@import 'tailwindcss'` в `src/index.css`); файлы `tailwind.config.js` / `postcss.config.js` в v4 не нужны.
- **Shadcn UI v4** (стиль `radix-nova`, `components.json`): вместо `clsx` + `tailwind-merge` используется пакет `cn` (shadcn-ui/cn), примитивы — из пакета `radix-ui`, шрифт Geist, тема — CSS-переменные в `src/index.css`. Алиас `@/*` → `src/*` (`jsconfig.json` + `vite.config.js`).
- `cosineSimilarity` и `chunkText` (плюс `normalizeVector`, `meanVector`) — в одном модуле **`src/utils/vectorMath.js`** (по указанию пользователя) вместо `src/services/similarity/` и `src/services/chunking/`.
- Демо-данные — **`src/mocks/semanticData.js`** (по указанию пользователя) вместо `src/services/mockData/fixtures.js`. Файл воспроизводит два эталона: видео (одностраничный анализ, `mockAnalysisResult`) и скриншот экрана Comparison (`mockComparisonMap`).
- **Transformers.js**: `@huggingface/transformers` v4 (официальный преемник устаревшего `@xenova/transformers`), модель `onnx-community/all-MiniLM-L6-v2-ONNX`. Подгружается динамическим `import()` только при выборе режима, поэтому не попадает в основной бандл.
- **Модель данных расширена** (`src/types/models.js`) под эталонный UI:
  - `Chunk.label` / `heading` / `blockType` для столбцов Content Chunk и Type (Paragraph / H2);
  - `AnalysisResult.sourceText` / `summary` (Avg Relevance, Overall Cohesion IDX, Optimization Needed);
  - `PageVector.clusterId` для переключателя Color: Site / Cluster;
  - `DeepAnalysis.toneAndReadability` и `suggestion.type` (`addition` / `structure` / `revision`);
  - тип `ComparisonMap`.
- **MockEmbeddingProvider** — детерминированный feature hashing (слова + символьные триграммы) с общей «тематической» осью, а не чистый рандом: в демо-режиме пользовательский текст получает осмысленный разброс по трём зонам.
- `tetherThreshold` по умолчанию **0.75** (скриншот: «Tether ≥ 7.5» по шкале 0–10).
- `npm audit`: 4 high-уязвимости в `onnxruntime-node` и `sharp` — это Node-only зависимости `@huggingface/transformers`, в браузерный бандл они не входят; исправления в upstream пока нет.

## Отклонения от плана (Phase 3)

- **Оркестрация запуска** вынесена в `src/services/analysisRunner.js`: загрузка контента → вырезка раздела → `analyzeContent`, плюс стадии для панели «Computing vectors». В стор добавлен экшен `runAnalysis(request)`. Форма вызывает его, а не `analysisPipeline` напрямую.
- **Загрузка URL из браузера** (`src/services/pageFetcher.js`): HTML превращается в текст с markdown-заголовками, чтобы сохранялись уровни H1–H6. Сайты без CORS загрузить не получится — пользователь видит понятную ошибку с советом использовать вкладку Text. Серверный прокси остаётся вне рамок (см. `docs/plan.md` §4).
- **Demo-режим не обращается к сети:**
  - любой URL загружает образец статьи, URL конкурентов получают заглушки по slug;
  - эталонный сценарий из видео (keyword + URL, Complete article, Layout) возвращает курируемый `mockAnalysisResult`;
  - шаги прогресса идут с паузой 450 мс, чтобы их было видно.
- **Новые опции формы** записываются в `AnalysisResult.options`:
  - **Content scope**: `complete_article` / `specific_section`. Раздел вырезается по заголовку вместе с вложенными подразделами (`src/utils/contentScope.js`);
  - **Vector algorithm**: `layout` / `semantic` (`src/services/semanticChunking.js`). Предложения эмбеддятся, чанк разрывается на самых больших расстояниях между соседями (верхняя четверть) или по лимиту длины; заголовок всегда остаётся со следующим за ним текстом.
- **Target Audience / Content Purpose / Website Niche** — Select с пресетами и пунктом «Custom…» для свободного текста: спецификация допускает оба варианта.
- **Переключатель режима эмбеддингов** — radio group из карточек с описанием режима вместо Tabs/Select.
- Добавлены shadcn-примитивы `input`, `textarea`, `label`, `select`, `tabs`, `radio-group` и dev-зависимость `jsdom` для тестов парсинга HTML.

## Отклонения от плана (Phase 4–5)

- **Comparison Map сделан на D3 + SVG, а не на React Flow** — по прямому указанию пользователя («2D граф на Canvas/D3»). Pan/zoom — `d3-zoom`, раскладка — `d3-force`. SVG выбран вместо Canvas: узлов ≤ ~100, а нужны DOM-события для hover и доступность.
- **Единая раскладка `radialLayout`** (`src/utils/vectorLayout.js`) для радара и Comparison Map:
  - радиус = (1 − cos) / maxDistance × R, точно;
  - угол — из 2D-PCA остатка вектора после вычета направления ключа, поэтому близкие подтемы оказываются рядом;
  - `d3-force` разводит пересечения, затем каждая точка возвращается на свой точный радиус.

  `d3.scaleRadial` не используется: радиус считается линейно по 1 − cos, как требует DoD P4-8.
- **Comparison Map, дополнительно к плану:**
  - переключатель набора данных Current analysis / Sample: Law Firm SEO (эталон со скриншота);
  - режим Color: Site / Cluster;
  - порог по шкале 0–10, как «Tether ≥ 7.5» на скриншоте, и кольца со значениями Tether;
  - панель со сводкой и слайдером — боковая колонка, а не оверлей поверх карты, чтобы не закрывать точки.

  «Gap ≤», «Fetch volumes» и «Export CSV» со скриншота не реализованы: их нет в спецификации.
- **Ось X графика** — метки чанков в порядке следования (как в видео), а не номера.
- **Цвета зон** (`color`, `tint`) вынесены в `RELEVANCE_ZONES` и общие для всех визуализаций.
- **shadcn `Slider`** доработан: пробрасывает `aria-label` / `aria-labelledby` на ползунок (`role=slider`). Без этого у слайдера нет доступного имени.
- **Бандл:** основной чанк ~842 kB (recharts + d3), Vite предупреждает о чанке > 500 kB. При необходимости визуализации можно вынести в `React.lazy`.

---

## Phase 1 — Инициализация проекта, Tailwind, базовый Layout и Mock-данные

- [x] **P1-1. Инициализация Vite + React проекта**
  DoD: `npm run dev` поднимает dev-server, в браузере отображается стартовая страница React без ошибок в консоли.
  Проверено: страница открывается в Browser preview; `npm run build` собирается без ошибок.

- [x] **P1-2. Установка и настройка TailwindCSS**
  Файлы: `vite.config.js` (`@tailwindcss/vite`), `src/index.css` — см. «Отклонения от плана» (Tailwind v4 без `tailwind.config.js`/`postcss.config.js`).
  DoD: утилитарные классы Tailwind, применённые в `App.jsx`/`AppShell.jsx`, визуально применяются в браузере.

- [x] **P1-3. Установка и инициализация Shadcn UI**
  DoD: `components.json` создан (`shadcn init`, Vite + Radix); сгенерированный `src/components/ui/button.jsx` рендерится в `App.jsx` (кнопка «Reload demo data»), сборка без ошибок.

- [x] **P1-4. Установка lucide-react**
  DoD: иконки `Radar`, `RotateCcw`, `SlidersHorizontal` из `lucide-react` отображаются в `AppShell`/`App`.

- [x] **P1-5. Создание базовой структуры директорий проекта**
  Файлы: `src/components/{config,visualizations,analysis,content,layout,ui}`, `src/services/embeddings`, `src/store`, `src/types`, `src/utils`, `src/mocks`, `src/lib`.
  DoD: директории созданы; в ещё пустых лежит `.gitkeep`. Вместо `services/{chunking,similarity,mockData}` используются `src/utils/vectorMath.js` и `src/mocks/` — см. «Отклонения от плана».

- [x] **P1-6. Компонент `AppShell.jsx`**
  Файлы: `src/components/layout/AppShell.jsx`.
  DoD: каркас из шапки (логотип, название, бейдж текущего режима эмбеддингов), слота настроек (`settings`) и основной области; подключён в `App.jsx`, виден в браузере.

- [x] **P1-7. Установка Zustand и создание сторов-заготовок**
  Файлы: `src/store/useAnalysisStore.js`, `src/store/useSettingsStore.js`.
  DoD: сторы с начальным состоянием: `analysisResult`, `status`, `stage`, `error`, `highlightedChunkId`, `embeddingMode: 'mock'`, `openaiApiKey: null`, `tetherThreshold: 0.75`, плюс экшены. Хуки используются в `App`/`AppShell` без ошибок.

- [x] **P1-8. Описание типов данных (JSDoc)**
  Файлы: `src/types/models.js`.
  DoD: все `@typedef` из раздела 3 плана на месте и расширены полями эталонного UI (см. «Отклонения от плана»); файл импортируется без ошибок (проверено `node -e "import('./src/types/models.js')"`).

- [x] **P1-9. Mock-данные, соответствующие схеме `AnalysisResult`**
  Файлы: `src/mocks/semanticData.js`, тест `src/mocks/semanticData.test.js`.
  DoD: экспортируется полный `AnalysisResult` (`mockAnalysisResult`):
  - 12 чанков, по всем трём зонам: 3 / 7 / 2;
  - 2 конкурента по 4 и 3 страницы;
  - заполненный `deepAnalysis`.

  Скоры чанков из видео воспроизведены: Intro 49%, Overview 54%, What does an accountant do? 61%, Best universities 44%, Conclusion 54%. Дополнительно — `mockComparisonMap` со скриншота: «Law Firm SEO», 29 + 44 = 73 страницы при Tether ≥ 7.5, best 8.1/10, кластеры для Color: Cluster. Эмбеддинги синтетические, но cosine similarity к ключу точно равна заявленным скорам (покрыто тестами).

- [x] **P1-10. Загрузка mock-данных в `useAnalysisStore` по умолчанию в demo-режиме**
  DoD: при `embeddingMode: 'mock'` стор стартует с `mockAnalysisResult`. Проверено через `useAnalysisStore.getState()` в браузере и по дашборду-обзору, который рендерится сразу после загрузки: Avg relevance 52.7%, Cohesion 58 IDX, 9 chunks to optimize.

---

## Phase 2 — Математический модуль (Chunking, Cosine Similarity, Embeddings Service)

- [x] **P2-1. Утилита `cosineSimilarity`**
  Файлы: `src/utils/vectorMath.js` (см. «Отклонения от плана»).
  DoD: принимает два массива одинаковой длины и возвращает значение в `[-1, 1]` (float-погрешность обрезается): для идентичных векторов `1`, для ортогональных `0`, для нулевого вектора `0`; при разной длине бросает ошибку. Покрыта тестами (P2-9).

- [x] **P2-2. Утилита классификации зон релевантности**
  Файлы: `src/utils/relevanceZones.js`.
  DoD: `classifyRelevanceZone(ratio)` принимает similarity как долю (0–1). Правила: >65% → `highly_relevant`, ≥43% → `broad_match`, иначе `noise`; дробные значения между опубликованными диапазонами уходят в нижнюю зону. Граничные 0.65, 0.651, 0.43 и 0.4299 проверены тестом.

- [x] **P2-3. Утилита `chunkText`**
  Файлы: `src/utils/vectorMath.js`.
  DoD: layout-based стратегия с параметрами `maxChunkLength` (600) и `minChunkLength` (80):
  - базовая единица — абзацы;
  - длинные абзацы режутся по предложениям, а одно сверхдлинное предложение — по пробелам;
  - короткие фрагменты (заголовки, хвосты) склеиваются с соседом.

  Возвращает `{ text, index, charStart, charEnd }`; инвариант `text.slice(charStart, charEnd) === chunk.text` проверен тестами, в том числе на CRLF и отступах.

- [x] **P2-4. Контракт `EmbeddingProvider`**
  Файлы: `src/services/embeddings/EmbeddingProvider.js`.
  DoD: JSDoc-контракт `{ id, label, embed(texts): Promise<number[][]> }` и фабрика `createEmbeddingProvider({ mode, openaiApiKey })`; все три провайдера его реализуют.

- [x] **P2-5. `MockEmbeddingProvider`**
  Файлы: `src/services/embeddings/mockEmbeddingProvider.js`.
  DoD: детерминированные единичные векторы размерности 256 (feature hashing, см. «Отклонения от плана»); одинаковый текст даёт одинаковый вектор (тест), тематически близкий текст оценивается выше постороннего (тест).

- [x] **P2-6. `TransformersEmbeddingProvider` (`@huggingface/transformers`)**
  Файлы: `src/services/embeddings/transformersEmbeddingProvider.js`.
  DoD: При вызове `embed(['test'])` в браузере (без сетевого ключа) модель загружается и возвращает числовой вектор ожидаемой размерности; повторный вызов после первой загрузки модели не вызывает повторную полную загрузку весов (проверка по логам/Network tab — используется кэш).
  Проверено в Browser preview:
  - `embed(['test'])` → вектор 384 с нормой 1.0000;
  - повторный `embed` на 3 текста — 33 мс, новых сетевых запросов нет (extractor кэшируется на модель, веса — в Cache Storage браузера);
  - similarity: близкий текст 0.458, посторонний 0.039.

  Особенность dev-режима: при первом `import()` Vite один раз пре-бандлит `@huggingface/transformers` и перезагружает вкладку.

- [ ] **P2-7. `OpenAIEmbeddingProvider` (`text-embedding-3-small`)**
  Файлы: `src/services/embeddings/openaiEmbeddingProvider.js`.
  DoD: При наличии валидного API-ключа (передаётся параметром, не хардкодится) запрос к OpenAI Embeddings API возвращает вектор; при отсутствии/невалидном ключе функция выбрасывает понятную ошибку, которая может быть поймана вызывающим кодом (не «тихий» сбой).
  Статус: реализовано. На замоканном `fetch` проверены:
  - формат запроса и порядок векторов;
  - понятные ошибки для отсутствующего ключа, 401 и сетевого сбоя.

  Не проверено: живой запрос с реальным ключом (ключа нет) — это последний пункт DoD.

- [x] **P2-8. Оркестрация `analysisPipeline`**
  Файлы: `src/services/analysisPipeline.js`.
  DoD: `analyzeContent({ keyword, sourceText, inputSource, meta, competitors, provider, chunking, onStage })` собирает `AnalysisResult`:
  - текст режется на чанки;
  - keyword, чанки и страницы конкурентов эмбеддятся одним вызовом `provider.embed`;
  - у каждого чанка есть `similarity`, `relevanceZone`, `label` / `heading` / `blockType`;
  - считаются `summary`, `targetPage` и страницы конкурентов.

  Стадии `chunking → embedding → scoring → aggregating` (шаги экрана «Computing Vectors») отдаются через `onStage`. Покрыто тестами.

- [x] **P2-9. Unit-тесты математического модуля**
  Файлы: `src/utils/vectorMath.test.js`, `src/utils/relevanceZones.test.js`, `src/services/embeddings/embeddingProviders.test.js`, `src/services/analysisPipeline.test.js`, `src/mocks/semanticData.test.js` (Vitest).
  DoD: `npm test` — 57 тестов зелёные. Граничные случаи покрыты: нулевые векторы, пустой текст, точные пороги зон, CRLF, сверхдлинные предложения.

---

## Phase 3 — Компоненты ввода данных и конфигурации анализа

- [x] **P3-1. Каркас `AnalysisForm.jsx`**
  Файлы: `src/components/config/AnalysisForm.jsx`, `src/components/config/PresetSelect.jsx`.
  DoD: Форма рендерит все поля из п. 3.1 спецификации (Target Keyword, переключатель URL/Текст, Target Audience, Content Purpose, Website Niche); форма встроена в `AppShell` и видна в браузере.
  Проверено: блок «Analysis configuration» в слоте настроек `AppShell` содержит:
  - поля Target keyword и URL / Text;
  - три селектора с пресетами и «Custom…»;
  - Content scope (Complete article / Specific section) и Vector algorithm (Layout-based / Semantic AI Chunking);
  - список конкурентов и кнопку «Run Semantic Analysis».

  Демо-значения из видео подставляются по умолчанию.

- [x] **P3-2. Переключатель источника контента URL / Текст**
  DoD: При выборе «URL» отображается текстовое поле для ссылки, при выборе «Текст» — textarea; переключение скрывает/очищает неиспользуемое поле; проверено кликом в браузере.
  Проверено в браузере: при выборе Text поле URL исчезает, появляется textarea со счётчиком символов. Введённое значение сохраняется при переключении туда-обратно, но в запрос уходит только активный источник (`buildAnalysisRequest`, покрыто тестом). Для URL есть кнопка Fetch: она показывает «Ready: N chars indexed» или ошибку загрузки.

- [x] **P3-3. Валидация обязательных полей**
  Файлы: `src/components/config/validateAnalysisForm.js` (+ тест).
  DoD: Попытка отправки формы с пустым Target Keyword или пустым источником контента блокируется, под соответствующим полем отображается текст ошибки; форма с валидными данными отправляется без ошибок.
  Проверки: keyword; URL (только http/https); текст ≥50 символов; заголовок для Specific section; URL конкурентов; ключ в режиме OpenAI. Проверено в браузере: пустой keyword и `example.com/page` блокируют отправку, ошибки выводятся под полями (`aria-invalid` + `aria-describedby`), фокус переходит на первое невалидное поле. После первой попытки ошибки пересчитываются по мере ввода.

- [x] **P3-4. Динамический список конкурентов**
  DoD: Пользователь может добавить/удалить произвольное количество строк URL конкурентов (кнопки «Добавить»/«Удалить»); пустые строки конкурентов не попадают в итоговый payload при отправке.
  Проверено в браузере: добавлено 3 строки, третья удалена, одна строка оставлена пустой — в анализ попала 1 страница конкурента. Лимит — 10 строк. Отбрасывание пустых строк покрыто тестом `buildAnalysisRequest`.

- [x] **P3-5. `SettingsPanel.jsx` — переключатель режима эмбеддингов**
  Файлы: `src/components/layout/SettingsPanel.jsx`.
  DoD: Компонент предоставляет выбор между Mock / Transformers.js / OpenAI (например, Shadcn `Tabs` или `Select`); выбор обновляет `useSettingsStore.embeddingMode`; текущий активный режим явно отображён в UI (текстовая метка или бейдж) — закрывает критерий п. 4.2 спецификации.
  Проверено в браузере: radio group Demo / Local AI (Transformers.js) / OpenAI обновляет стор. Активный режим показан дважды: «Active: …» в панели и бейдж в шапке («Demo mode · mock data», «Transformers.js · local», «OpenAI embeddings»). Во время прогона переключатель заблокирован.

- [x] **P3-6. Поле ввода OpenAI API key**
  DoD: Поле типа `password` отображается только при выборе режима OpenAI; значение хранится исключительно в памяти стора (`useSettingsStore`), не сохраняется в `localStorage`/`sessionStorage` и не попадает в URL — проверено вручную (DevTools → Application → Storage пуст после ввода ключа).
  Проверено в браузере: поле `type="password"` появляется только в режиме OpenAI и скрывается при возврате в Demo. После ввода ключа `localStorage` и `sessionStorage` пусты (0 записей), cookie пусты, ключа нет в URL.

- [x] **P3-7. Связка `AnalysisForm` → `analysisPipeline` → `useAnalysisStore`**
  DoD: Отправка формы с валидными данными вызывает `analysisPipeline` с выбранным в `SettingsPanel` провайдером и записывает результат в `useAnalysisStore.analysisResult`; проверено вручную для режима Mock (P2-5) — после сабмита стор содержит новый `AnalysisResult`, соответствующий введённым данным формы.
  Путь запуска: форма → `useAnalysisStore.runAnalysis` → `runAnalysisRequest` (провайдер из настроек) → `analyzeContent` → `analysisResult` (см. «Отклонения от плана (Phase 3)»). Проверено в браузере:
  - Demo, эталон из видео → 52.7% / 58 IDX / 9 chunks;
  - Demo, Text + Specific section «Accountancy apprenticeships» + Semantic AI Chunking → 2 чанка и 1 страница конкурента, опции отражены в обзоре;
  - Transformers.js на вставленном тексте → «Transformers.js vectors», 3 чанка.

  Покрыто тестами `analysisRunner.test.js` и `useAnalysisStore.test.js`.

- [x] **P3-8. Индикация состояния загрузки и ошибок анализа**
  Файлы: `src/components/config/AnalysisProgress.jsx`.
  DoD: Во время выполнения `analysisPipeline` в UI отображается индикатор загрузки (spinner/skeleton); при выбросе ошибки (например, невалидный OpenAI-ключ из P2-7) пользователю показывается читаемое сообщение об ошибке вместо «зависшего» состояния формы.
  Проверено в браузере:
  - во время прогона кнопка показывает спиннер и «Analyzing content…», форма заблокирована (`fieldset disabled`);
  - панель «Computing vectors» проходит 5 шагов, от Fetching content до Aggregating results;
  - при ошибке появляется alert «Analysis failed» с текстом, а прежний результат остаётся на экране. Проверено на недоступном URL в режиме Transformers.js (сообщение про CORS). Отсутствующий ключ OpenAI ловится ещё до запуска.

---

## Phase 4 — RelevanceChart и ProximityRadar

- [x] **P4-1. Установка Recharts**
  DoD: Пакет добавлен в `package.json`; минимальный `LineChart` с тестовыми данными рендерится в браузере без ошибок.
  Проверено: `recharts@3.10.1`, `LineChart` в `RelevanceChart` рендерится, в консоли ошибок нет.

- [x] **P4-2. Базовый `RelevanceChart.jsx`, привязанный к данным из стора**
  Файлы: `src/components/visualizations/RelevanceChart.jsx`.
  DoD: Компонент читает `analysisResult.chunks` из `useAnalysisStore` и рендерит линию по оси X = порядковый номер чанка, по оси Y = `similarity` (%); при подстановке mock-данных из P1-9 график визуально отображает разброс значений.
  Проверено: 12 точек mock-анализа идут в порядке следования чанков (ось X подписана метками чанков, см. «Отклонения»), Y — similarity в %. Точки окрашены по зоне.

- [x] **P4-3. Визуальные зоны релевантности на графике**
  DoD: На графике отображены три горизонтальные зоны (через `ReferenceArea`/аналог) с границами >65%, 43–64%, 0–42%, визуально различимые по цвету/заливке — соответствует п. 3.4 спецификации.
  Проверено: `ReferenceArea` 0–43% (красная), 43–65% (жёлтая), 65–100% (зелёная). Деления оси Y стоят на границах: 0 / 43 / 65 / 100%. Над графиком — легенда зон.

- [x] **P4-4. Hover-tooltip на `RelevanceChart`**
  DoD: При наведении на точку графика отображается tooltip с фрагментом текста чанка и точным значением similarity; проверено вручную наведением курсора в браузере.
  Проверено наведением курсора: «Best universities for accountancy courses | 44% · Broad Match | League tables rank…».

- [x] **P4-5. Клик по точке `RelevanceChart` — выделение чанка**
  DoD: Клик по точке записывает `id` чанка в `useAnalysisStore.highlightedChunkId`; проверено через React DevTools — значение в сторе меняется при клике.
  Проверено настоящим кликом мыши по точке: значение уходит в `highlightedChunkId`. Контроль шёл по DOM вместо DevTools — точка графика получает кольцо, соответствующая точка радара — `aria-pressed="true"`. Повторный клик снимает выделение.

- [x] **P4-6. Установка D3.js**
  DoD: Пакет добавлен в `package.json`; тестовый вызов `d3.scaleRadial` в консоли/тестовом компоненте не выбрасывает ошибок.
  Проверено: `d3@7.9.0` работает в браузере и в тестах. Используются `d3-force` (раскладка) и `d3-zoom` (Comparison Map). Вместо `scaleRadial` радиус считается линейно, см. «Отклонения».

- [x] **P4-7. Каркас `ProximityRadar.jsx` — SVG с радиальной шкалой и кольцами зон**
  Файлы: `src/components/visualizations/ProximityRadar.jsx`.
  DoD: Компонент рендерит SVG с центральной точкой и концентрическими кольцами, соответствующими границам зон релевантности (аналогично P4-3); кольца видны в браузере при подстановке mock-данных.
  Проверено: концентрические зоны-кольца на расстояниях 1 − cos = 0.35 (65%), 0.57 (43%) и 1 (0%), подкрашены цветом зоны и подписаны процентами, плюс перекрестие. Внизу легенда: Query / зоны / «r ∝ 1 − cos θ».

- [x] **P4-8. Отрисовка центрального узла Keyword и точек чанков**
  DoD: Центральный узел визуально выделен (иная форма/размер/цвет, подписан текстом Target Keyword); каждая точка-чанк расположена на расстоянии от центра, пропорциональном `1 - similarity` (чем выше similarity — тем ближе к центру); расположение по углу — равномерное или детерминированное (без наложения точек друг на друга при типичном количестве чанков ~10–30).
  Проверено:
  - центр выделен: фиолетовый узел с ореолом, подпись с ключевым словом (длинные обрезаются), полный текст — в подсказке;
  - радиус точки равен (1 − similarity) × R — это проверено тестом, и в браузере точки лежат в своих зонах;
  - угол детерминирован (2D-PCA), наложения разводит `d3-force` — тесты `vectorLayout.test.js`: детерминизм и разнесение дублей.

- [x] **P4-9. Hover-tooltip на `ProximityRadar`**
  DoD: При наведении на точку-чанк отображается tooltip с текстом чанка и значением similarity — аналогично критерию п. 4.1 спецификации.
  Проверено: «Routes into accountancy in the UK | 78% · Highly Relevant | There are three common ways…». Та же подсказка появляется при фокусе с клавиатуры.

- [x] **P4-10. Клик по точке `ProximityRadar` — синхронизация выделения чанка**
  DoD: Клик по точке в `ProximityRadar` и клик по точке в `RelevanceChart` записывают значение в один и тот же `highlightedChunkId`; выделение, сделанное в одном компоненте, визуально отражается (например, обводка/цвет) и во втором компоненте без перезагрузки страницы.
  Проверено в обе стороны:
  - клик по точке радара → кольцо на соответствующей точке графика;
  - клик по графику → `aria-pressed` и кольцо на радаре.

  Точки радара доступны с клавиатуры (Tab, затем Enter/Space); повторное нажатие снимает выделение.

- [x] **P4-11. Адаптивность графиков**
  DoD: `RelevanceChart` и `ProximityRadar` корректно масштабируются при изменении ширины окна браузера (ResizeObserver/`ResponsiveContainer` для Recharts, аналогичный подход для SVG D3) — проверено ресайзом окна от 1440px до 768px без визуальных поломок (обрезанные оси, наложение точек).
  Масштабирование: `ResponsiveContainer` у Recharts, SVG через `viewBox`. Проверено в браузере:
  - 1440 px — радар (480 px) и график (490 px) стоят рядом, Comparison Map — 830 px;
  - 768 px — блоки один под другим (график 655 px);
  - горизонтальной прокрутки нет ни в одном случае.

---

## Phase 5 — Модуль Comparison Map (векторный граф с Tether-слайдером)

- [x] **P5-1. ~~Установка React Flow~~ → D3 (заменено)**
  DoD (исходный): Пакет добавлен в `package.json`; минимальный граф с двумя тестовыми узлами и одним ребром рендерится в браузере с работающим pan/zoom.
  Статус: по указанию пользователя граф сделан на D3 + SVG, React Flow не устанавливался (см. «Отклонения от плана (Phase 4–5)»). Pan/zoom обеспечивает `d3-zoom`, проверено в P5-10.

- [x] **P5-2. Утилита радиальной раскладки узлов по cosine similarity**
  Файлы: `src/utils/vectorLayout.js` (+ тест).
  DoD: Функция принимает список `PageVector` (включая `similarityToKeyword`) и возвращает координаты `{x, y}` для каждого узла, где расстояние от центра (позиции keyword-узла) монотонно убывает с ростом similarity; координаты не пересекаются полностью (базовое угловое распределение) при ≥10 узлах на входе.
  `radialLayout(vectors, keywordEmbedding, { radius, maxDistance, markerRadius })` работает на эмбеддингах страниц. Тесты подтверждают:
  - расстояние от центра = (1 − cos) / maxDistance × R, то есть монотонно убывает с ростом similarity;
  - раскладка детерминирована;
  - совпадающие векторы разводятся;
  - на 91 странице эталона страницы одного кластера получают близкие углы.

- [x] **P5-3. Узел-ромб для Target Keyword**
  DoD (адаптирован под D3): золотой ромб с подписью ключевого слова, визуально отличимый от круглых узлов страниц.
  Проверено: золотой ромб (`#f0b429`, тёмный контур) в центре карты, под ним подпись ключевого слова, при наведении — подсказка «Target keyword». Тот же ромб повторяется в заголовке боковой панели.

- [x] **P5-4. Узел-точка для страниц (target vs competitor)**
  DoD: Страницы целевого сайта и страницы каждого конкурента рендерятся точками разного цвета (цвет конкурента берётся из `CompetitorSite.color`); при наведении на узел отображается tooltip с `label`/`url` и `similarityToKeyword` — закрывает часть критерия п. 4.1 (hover).
  Проверено: свои страницы — зелёные (`TARGET_SITE_COLOR`), страницы конкурентов — цветом из `CompetitorSite.color`. Подсказка при наведении: «yourfirm-seo.example.com | Law firm site speed guide | https://… | Tether 7.6/10 · cos 0.759».

- [x] **P5-5. Ребро Tether**
  DoD: Линия, соединяющая keyword-узел с каждым узлом страницы; толщина и/или прозрачность линии визуально коррелирует со значением similarity этой пары (выше similarity — заметнее линия).
  Проверено: линии от ромба к страницам цветом страницы. Толщина (0.75–2.25) и прозрачность (0.2–0.75) растут с similarity выше порога; `vector-effect: non-scaling-stroke` сохраняет толщину при зуме.

- [x] **P5-6. `ComparisonVectorMap.jsx` — сборка графа из данных стора**
  Файлы: `src/components/visualizations/ComparisonVectorMap.jsx`, `src/utils/comparisonModel.js` (+ тест).
  DoD: Компонент строит полный список узлов (`targetPage` + все страницы из `competitors`) и рёбер (`keyword ↔ каждая страница`), используя P5-2–P5-5; при подстановке mock-данных из P1-9 граф отображается в браузере со всеми ожидаемыми узлами.
  Проверено: «Current analysis» строится из стора (`targetPage` + 7 страниц конкурентов = 8 узлов). «Sample: Law Firm SEO» (`mockComparisonMap`) даёт 91 узел и воспроизводит скриншот: 73 из 91 страниц ≥ 7.5, 29 / 44 по сайтам, best 8.1/10.

- [x] **P5-7. Слайдер Tether threshold**
  DoD: В UI (Shadcn `Slider`) добавлен элемент управления порогом в диапазоне 0–100% (или 0–1), значение записывается в `useSettingsStore.tetherThreshold`; текущее значение порога отображается рядом со слайдером в виде числа/процента.
  Проверено: shadcn `Slider` в шкале 0–10 с шагом 0.1 (как «Tether ≥ 7.5» на скриншоте) пишет в стор `tetherThreshold` = значение / 10. Текущее значение показано рядом, у ползунка есть доступное имя (`aria-labelledby`), управление с клавиатуры работает.

- [x] **P5-8. Фильтрация рёбер по порогу в реальном времени**
  DoD: Рёбра, у которых `similarity < tetherThreshold`, скрываются из графа сразу при изменении слайдера — без повторного вызова layout-утилиты (P5-2) и без перезагрузки страницы; проверено вручную — перемещение слайдера от 0 до 1 плавно скрывает/показывает линии. Закрывает критерий п. 4.1 спецификации (Tether threshold filtering).
  Проверено в браузере на эталоне:
  - Home (0.0) → 91 линия;
  - End (10.0) → 0 линий;
  - 25 × ← (7.5) → 73 линии.

  Координаты всех 91 узла при этом не изменились: раскладка мемоизирована только от данных. Страницы ниже порога приглушаются.

- [x] **P5-9. Легенда графа**
  DoD: Под/над графом отображается легенда, сопоставляющая цвет точки с названием сайта (целевой сайт + каждый конкурент по имени и цвету из `CompetitorSite.color`).
  Проверено: в боковой панели каждый сайт показан строкой «цвет — имя (you) — число страниц ≥ порога»: «yourfirm-seo.example.com (you) 29», «legalrank.example.com 44». В режиме Color: Cluster легенда показывает кластеры (14 / 17 / 10 / 15 / 17).

- [x] **P5-10. Проверка pan/zoom и общей отзывчивости графа**
  DoD: Граф корректно поддерживает зум колесом мыши и перетаскивание фона (pan) без потери позиций узлов; проверено вручную в браузере при ≥15 узлах на графе (несколько конкурентов).
  Проверено на 91 узле:
  - кнопка «+» → scale 1.4, колесо → 2.44, перетаскивание сдвигает вид, «1x» → `translate(0,0) scale(1)`;
  - координаты узлов не меняются, трансформация применяется к общей группе.

  Двойной клик для зума отключён, чтобы не срабатывать случайно.

---

## Phase 6 — Deep AI Analysis и детализация чанков

- [ ] **P6-1. `deepAnalysisService` — mock-генератор**
  Файлы: `src/services/deepAnalysisService.js`.
  DoD: При режиме Mock функция возвращает объект `DeepAnalysis` (Executive Summary, Missing Entities, Suggestions) на основе входных `AnalysisResult` (например, простые эвристики по распределению зон релевантности), без сетевых вызовов.

- [ ] **P6-2. `deepAnalysisService` — вызов реального AI-анализа**
  DoD: При режиме, отличном от Mock (и наличии необходимого API-доступа), функция формирует запрос к LLM с промптом, включающим keyword, meta-поля и содержимое чанков, и парсит ответ в структуру `DeepAnalysis`; при ошибке сети/парсинга выбрасывает понятную ошибку, перехватываемую UI (аналогично P3-8).

- [ ] **P6-3. Карточка Executive Summary**
  Файлы: `src/components/analysis/DeepAnalysisCards.jsx`.
  DoD: Компонент отображает текст `deepAnalysis.executiveSummary` в отдельной карточке (Shadcn `Card`); карточка видна в браузере при заполненных mock-данных.

- [ ] **P6-4. Облако тегов Missing Entities**
  DoD: Каждый элемент `deepAnalysis.missingEntities` рендерится как тег во flex-обёртке; при большом количестве сущностей (проверено на ≥15 элементах) блок не ломает layout страницы (перенос строк работает корректно).

- [ ] **P6-5. Список Actionable Optimization Suggestions**
  DoD: Каждая рекомендация из `deepAnalysis.suggestions` отображается с заголовком, описанием и визуальным индикатором приоритета (`high`/`medium`/`low` — например, цветной бейдж); порядок отображения — от высокого приоритета к низкому.

- [ ] **P6-6. `SourceTextPanel.jsx` — отображение полного текста с границами чанков**
  Файлы: `src/components/content/SourceTextPanel.jsx`.
  DoD: Компонент рендерит полный исходный текст, визуально размеченный по границам `chunk.charStart`/`chunk.charEnd` (например, `<span>` на чанк); при наведении на чанк в тексте курсор/стиль показывает, что фрагмент кликабелен/связан с графиками.

- [ ] **P6-7. Подсветка выбранного чанка**
  DoD: При изменении `useAnalysisStore.highlightedChunkId` (из P4-5/P4-10) соответствующий `<span>` в `SourceTextPanel` получает выделяющий стиль (фон/рамка); снятие выделения (клик вне точки/повторный клик) возвращает обычный стиль — закрывает критерий п. 4.1 спецификации («клик по чанку → подсветка текста»).

- [ ] **P6-8. Автоскролл к выбранному чанку**
  DoD: При выделении чанка через график панель `SourceTextPanel` автоматически прокручивается так, чтобы выделенный фрагмент был виден в области видимости (`scrollIntoView`), без резких скачков страницы целиком.

---

## Phase 7 — Интеграция, полировка UI/UX и тесты

- [ ] **P7-1. Сквозная проверка потока данных на mock-данных**
  DoD: Полный цикл — заполнение `AnalysisForm` → сабмит → обновление `RelevanceChart`, `ProximityRadar`, `ComparisonVectorMap`, `DeepAnalysisCards`, `SourceTextPanel` — выполняется без ошибок в консоли браузера и без несоответствий данных между компонентами (например, одинаковое количество чанков во всех визуализациях).

- [ ] **P7-2. Обработка пустых и граничных состояний**
  DoD: При отсутствии результата анализа (`analysisResult === null`, начальное состояние без Mock-автозагрузки) каждый визуализационный компонент показывает осмысленное пустое состояние (placeholder/сообщение), а не пустой экран или JS-ошибку.

- [ ] **P7-3. Адаптивная вёрстка (desktop/tablet)**
  DoD: Основные экраны (форма ввода, дашборд с графиками, карточки Deep Analysis) остаются пригодными для использования при ширине вьюпорта 1024px и 1280px без горизонтального скролла всей страницы (локальный скролл внутри графов допустим).

- [ ] **P7-4. Базовая проверка доступности (a11y)**
  DoD: Все интерактивные элементы формы имеют связанные `<label>`; слайдер Tether threshold и переключатель режима доступны с клавиатуры (Tab + Enter/Arrow keys); контраст текста на карточках соответствует минимально читаемому уровню (визуальная проверка).

- [ ] **P7-5. Ручная проверка чек-листа критериев приёмки спецификации**
  DoD: Пройдены и отмечены все пункты раздела 4 `docs/spec.md`: hover на всех трёх графиках (4.1), фильтрация по Tether threshold в реальном времени (4.1), клик по чанку с подсветкой текста (4.1), рабочий Mock-режим (4.2), рабочее переключение на Transformers.js и/или OpenAI (4.2).

- [ ] **P7-6. Unit-тесты для сервисного слоя**
  DoD: Помимо тестов из P2-9, добавлены тесты для `analysisPipeline` (корректная сборка `AnalysisResult` из фиктивного `EmbeddingProvider`) и для layout-утилиты Comparison Map (P5-2); все тесты зелёные при `npm run test`.

- [ ] **P7-7. Проверка интерактивных сценариев (компонентные/интеграционные тесты)**
  DoD: Добавлены тесты, проверяющие: изменение `tetherThreshold` скрывает/показывает рёбра графа (P5-8); клик по чанку в одном графике обновляет выделение в другом (P4-10); переключение режима в `SettingsPanel` меняет активный `EmbeddingProvider`, используемый при сабмите формы.

- [ ] **P7-8. Финальная проверка production-сборки**
  DoD: `npm run build` завершается без ошибок и без критических предупреждений; собранный `dist/` открывается локально (например, через `vite preview`) и демонстрирует полный сценарий из P7-1.

- [ ] **P7-9. Сверка реализации с `docs/spec.md` и `docs/plan.md`**
  DoD: Пройден построчный обзор обоих документов; все функциональные требования из спецификации отражены в реализованных компонентах; отклонения от плана (если были) задокументированы (например, отдельным разделом «Отклонения от плана» в этом файле или в PR-описании).
